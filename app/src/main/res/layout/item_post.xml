<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="post"
            type="com.sammengistu.stuckfirebase.models.PostModel" />

        <variable
            name="viewMode"
            type="Integer" />

        <variable
            name="starred"
            type="Boolean" />

        <variable
            name="isHidden"
            type="Boolean" />

        <variable
            name="handler"
            type="com.sammengistu.stuckapp.handler.PostAdapterEventHandler" />
    </data>


    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="7dp"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="5dp"
            android:orientation="vertical"
            android:padding="3dp">

            <include
                layout="@layout/top_portion_post"
                bind:handler="@{handler}"
                bind:isHidden="@{isHidden}"
                bind:post="@{post}"
                bind:starred="@{starred}"
                bind:viewMode="@{viewMode}" />

            <LinearLayout
                android:id="@+id/choice_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/light_gray"
                android:orientation="vertical"
                android:padding="2dp"
                android:visibility="@{isHidden ? View.GONE:View.VISIBLE}"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>